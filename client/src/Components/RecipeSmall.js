import React from "react";
import styled from "styled-components";

const RecipeIcon = () => {
  <Icon>This is the small recipe li in the results ul </Icon>;
};

const Icon = styled.div``;

export default RecipeIcon;
